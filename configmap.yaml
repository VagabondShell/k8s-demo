apiVersion: v1
kind: ConfigMap
metadata:
  name: app-settings
  namespace: default
data:
  database_url: "jdbc:mysql://localhost:3306/db"
  max_connections: "10"
  app.properties: |
    environment=production
    debug=false
